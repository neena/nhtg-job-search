!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    %title= content_for?(:title) ? yield(:title) : "Job Site"
    = stylesheet_link_tag :application, :media => "all"
    = stylesheet_link_tag :application, "//fonts.googleapis.com/css?family=Nunito:300,400,700"
    = javascript_include_tag :application
    = csrf_meta_tags
    = yield(:head)
  %body
    .container
      #main
        = yield